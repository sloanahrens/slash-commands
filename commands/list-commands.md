---
description: List all available slash commands
---

# List Commands

Read and display the contents of `README.md` in this commands directory.

```bash
cat ~/.claude/commands/README.md 2>/dev/null || cat .claude/commands/README.md
```

Display the full README to the user - it contains the complete command reference, configuration guide, and plugin recommendations.
